import test from 'node:test'; import assert from 'node:assert/strict'; import { MemoryStore } from '../src/store.js'; import { Orchestrator } from '../src/orchestrator.js'; import { UnavailableCodex, UnavailableSpecialist, SafePromptBuilder, UnavailableValidation } from '../src/executors.js';
test('unavailable execution never fabricates completion',async()=>{const o=new Orchestrator(new MemoryStore(),new UnavailableSpecialist(),new SafePromptBuilder(),new UnavailableCodex(),new UnavailableValidation());const context={principal:{id:'local',roles:[],scopes:[],authType:'local' as const},operation:'create',requestId:'00000000-0000-0000-0000-000000000001',specialistId:'architecture-security-advisor',executionType:'codex',effectiveClassification:'PUBLIC' as const,resolvedRepository:{repositoryId:'repo',source:'./repo',classification:'PUBLIC' as const},authorizationDecision:{allowed:true as const,ruleId:'test',decidedAt:new Date().toISOString()}};const w=await o.create({context,workflowInput:{request_id:context.requestId,workflow_type:'software',requested_specialist:'architecture-security-advisor',objective:'x'}});const runContext={...context,operation:'run',workflowId:w.id};await o.run(w.id,runContext);assert.ok(['FAILED','MANUAL_HANDOFF_REQUIRED'].includes(w.status));assert.equal((await o.store.getEvents(w.id)).some(e=>e.eventType.includes('SPECIALIST_COMPLETE')),false);assert.equal((await o.store.getEvents(w.id)).some(e=>e.eventType.includes('CODEX_COMPLETE')),false);});
