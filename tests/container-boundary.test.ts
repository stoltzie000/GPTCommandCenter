import test from 'node:test'; import assert from 'node:assert/strict'; import { RootlessContainerCodexExecutor } from '../src/executors.js';
test('sandbox executor fails closed on non-Linux and never delegates to host executor',async()=>{const e=new RootlessContainerCodexExecutor('test-image','missing-runtime','./workspaces');const r=await e.execute({workflowId:'w',stageId:'s',trustedContext:{principal:{id:'caller',roles:[],scopes:[],authType:'token'},operation:'run',requestId:'r',workflowId:'w',specialistId:'architecture-security-advisor',executionType:'codex',effectiveClassification:'PUBLIC',resolvedRepository:{repositoryId:'repo',source:'./repo',classification:'PUBLIC'},authorizationDecision:{allowed:true,ruleId:'test',decidedAt:new Date().toISOString()}},promptArtifact:{task_summary:'x',implementation_instructions:'x',scope_constraints:[],tests_required:[],acceptance_criteria:[],expected_result_report:[]},executionPolicy:{}});if(process.platform!=='linux'){assert.equal(r.kind,'terminal_failure');assert.equal(r.errorCode,'DEPENDENCY_UNAVAILABLE');}});
