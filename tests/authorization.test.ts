import test from 'node:test'; import assert from 'node:assert/strict'; import { authorize, effectiveClassification } from '../src/authorization.js';
const repo={source:'C:\\approved',classification:'INTERNAL' as const,allowedCallers:['caller'],allowedSpecialists:['architecture-security-advisor'],allowedExecutionTypes:['specialist']};
test('classification cannot be downgraded',()=>{assert.equal(effectiveClassification('CONFIDENTIAL','PUBLIC'),'CONFIDENTIAL');assert.equal(effectiveClassification('INTERNAL','CONFIDENTIAL'),'CONFIDENTIAL');});
test('authorization is deny by default for disallowed dimensions',()=>{authorize({caller:'caller',operation:'run',specialistId:'architecture-security-advisor',repositoryId:'repo',executionType:'specialist',dataClassification:'INTERNAL',repository:repo});assert.throws(()=>authorize({caller:'other',operation:'run',specialistId:'architecture-security-advisor',repositoryId:'repo',executionType:'specialist',dataClassification:'INTERNAL',repository:repo}),/AUTHORIZATION_FAILED/);assert.throws(()=>authorize({caller:'caller',operation:'run',specialistId:'architecture-security-advisor',repositoryId:'repo',executionType:'codex',dataClassification:'INTERNAL',repository:repo}),/AUTHORIZATION_FAILED/);});
